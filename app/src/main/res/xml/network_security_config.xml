<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Tailscale is the security perimeter (WireGuard). Cleartext is safe on this VPN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>